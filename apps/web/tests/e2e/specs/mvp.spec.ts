import { test } from '@playwright/test';

test.describe('Flujos MVP', () => {
  test('crear plan 3x semana con duplicar días', async () => {
    // Placeholder: en un proyecto real se automatiza la UI del wizard.
  });

  test('cancelar clase del lunes', async () => {
    // Placeholder de E2E pendiente.
  });

  test('alumno recupera en miércoles', async () => {
    // Placeholder de E2E pendiente.
  });
});
